import { useEffect, useMemo, useState } from "react";
import { AlertTriangle, CalendarDays, CheckCircle2, ClipboardCheck, Circle, Clock3, Plus, Trash2 } from "lucide-react";
import { useSearchParams } from "react-router-dom";
import api from "../api";

type Project={id:number;name:string}; type Member={id:number;name:string;email:string;role:string};
type Task={id:number;title:string;description:string;status:string;priority:string;projectId:number;projectName:string;dueDate?:string|null;assignee?:{id:number;name:string;email:string;role:string}|null}; type StoredUser={id?:number;role?:string};
const columns=["Todo","In Progress","Review","Blocked","Done"]; const statuses=[...columns];
const statusIcon=(status:string)=>status==="Done"?<CheckCircle2 size={16}/>:status==="In Progress"?<Clock3 size={16}/>:status==="Review"?<ClipboardCheck size={16}/>:status==="Blocked"?<AlertTriangle size={16}/>:<Circle size={16}/>;
function currentUser():StoredUser{try{return JSON.parse(localStorage.getItem("nexora_user")||"{}") as StoredUser;}catch{return {};}}
function formatDate(value?:string|null){if(!value)return "No due date";const d=new Date(value);return Number.isNaN(d.getTime())?value:d.toLocaleDateString(undefined,{day:"numeric",month:"short",year:"numeric"});}
export default function Tasks(){const[params]=useSearchParams();const projectFromUrl=params.get("project")||"";const[tasks,setTasks]=useState<Task[]>([]);const[projects,setProjects]=useState<Project[]>([]);const[members,setMembers]=useState<Member[]>([]);const[form,setForm]=useState({title:"",description:"",projectId:projectFromUrl,assigneeId:"",priority:"Medium",dueDate:""});const[filters,setFilters]=useState({projectId:projectFromUrl,assigneeId:"",status:"",priority:""});const[error,setError]=useState("");const user=currentUser();const canCreate=user.role==="Admin"||user.role==="Manager";const canManageAll=canCreate;const load=async()=>{try{const[t,p,team]=await Promise.all([api.get("/tasks"),api.get("/projects"),api.get("/team")]);setTasks(t.data.tasks||[]);setProjects(p.data.projects||[]);setMembers(team.data.members||[]);}catch(err:any){setError(err.response?.data?.message||"Could not load tasks.");}};useEffect(()=>{void load();},[]);useEffect(()=>{if(projectFromUrl){setForm(f=>({...f,projectId:projectFromUrl}));setFilters(f=>({...f,projectId:projectFromUrl}));}},[projectFromUrl]);async function addTask(){if(!canCreate||!form.title.trim()||!form.projectId)return;try{await api.post("/tasks",{title:form.title.trim(),description:form.description.trim(),projectId:Number(form.projectId),priority:form.priority,assigneeId:form.assigneeId?Number(form.assigneeId):null,dueDate:form.dueDate||null,status:"Todo"});setForm(f=>({...f,title:"",description:"",assigneeId:"",dueDate:""}));await load();}catch(err:any){setError(err.response?.data?.message||"Could not create task.");}}async function changeStatus(task:Task,status:string){if(!(canManageAll||task.assignee?.id===user.id)||status===task.status)return;try{await api.patch(`/tasks/${task.id}`,{status});await load();}catch(err:any){setError(err.response?.data?.message||"Could not update task.");}}async function remove(id:number){if(!canManageAll)return;try{await api.delete(`/tasks/${id}`);await load();}catch(err:any){setError(err.response?.data?.message||"Could not delete task.");}}const filteredTasks=useMemo(()=>tasks.filter(t=>(!filters.projectId||String(t.projectId)===filters.projectId)&&(!filters.assigneeId||String(t.assignee?.id||"")===filters.assigneeId)&&(!filters.status||t.status===filters.status)&&(!filters.priority||t.priority===filters.priority)),[tasks,filters]);const selectedProject=projects.find(p=>String(p.id)===filters.projectId);return <div className="workspace-page"><div className="page-heading"><div><p className="eyebrow">Execution</p><h2>Tasks</h2><p>{selectedProject?`Tasks for ${selectedProject.name}.`:"Every task belongs to a project, has an owner, a status, a priority and an optional deadline."}</p></div><span className="role-badge">{filteredTasks.length} shown · {tasks.length} total</span></div>{error&&<div className="form-error page-alert">{error}</div>}{canCreate&&<div className="panel create-panel"><div><p className="eyebrow">Quick add</p><h3>Create a task</h3><p>Tasks start as Todo. Assign a workspace member and add a deadline when needed.</p></div><div className="form-row"><input className="workspace-input" value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="Task name"/><input className="workspace-input" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} placeholder="Description (optional)"/><select className="workspace-input" value={form.projectId} onChange={e=>setForm({...form,projectId:e.target.value})}><option value="">Select project</option>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><select className="workspace-input" value={form.assigneeId} onChange={e=>setForm({...form,assigneeId:e.target.value})}><option value="">Unassigned</option>{members.map(m=><option key={m.id} value={m.id}>{m.name} ({m.role})</option>)}</select><select className="workspace-input" value={form.priority} onChange={e=>setForm({...form,priority:e.target.value})}><option>Low</option><option>Medium</option><option>High</option><option>Critical</option></select><input className="workspace-input" type="date" value={form.dueDate} onChange={e=>setForm({...form,dueDate:e.target.value})} aria-label="Due date"/><button className="primary-button" onClick={addTask}><Plus size={15}/> Add task</button></div></div>}{!canCreate&&<div className="panel permission-note"><strong>Member access</strong><span>You can update tasks assigned to you. Managers and admins assign work and review completed tasks.</span></div>}<div className="panel task-filters"><select className="workspace-input" value={filters.projectId} onChange={e=>setFilters({...filters,projectId:e.target.value})}><option value="">All projects</option>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><select className="workspace-input" value={filters.assigneeId} onChange={e=>setFilters({...filters,assigneeId:e.target.value})}><option value="">All assignees</option>{members.map(m=><option key={m.id} value={m.id}>{m.name}</option>)}</select><select className="workspace-input" value={filters.status} onChange={e=>setFilters({...filters,status:e.target.value})}><option value="">All statuses</option>{statuses.map(s=><option key={s}>{s}</option>)}</select><select className="workspace-input" value={filters.priority} onChange={e=>setFilters({...filters,priority:e.target.value})}><option value="">All priorities</option>{["Low","Medium","High","Critical"].map(p=><option key={p}>{p}</option>)}</select></div><div className="task-board-scroll"><div className="task-columns">{columns.map(status=><div className="task-column" key={status}><div className="column-heading"><span className="status-heading">{statusIcon(status)}<span>{status}</span></span><strong>{filteredTasks.filter(t=>t.status===status).length}</strong></div>{filteredTasks.filter(t=>t.status===status).map(task=>{const canChange=canManageAll||task.assignee?.id===user.id;return <div className="task-card panel" key={task.id}><div className="task-card-top"><span className="task-status-icon">{statusIcon(status)}</span>{canManageAll&&<button className="danger-button" onClick={()=>remove(task.id)} aria-label={`Delete ${task.title}`}><Trash2 size={13}/></button>}</div><strong>{task.title}</strong>{task.description&&<span className="task-description">{task.description}</span>}<span className="task-project">{task.projectName}</span><span>{task.assignee?`Assigned to ${task.assignee.name}`:"Unassigned"}</span><span className="task-due-line"><CalendarDays size={12}/> {formatDate(task.dueDate)}</span><div className="task-card-footer"><select className="task-status-select" disabled={!canChange} value={task.status} onChange={e=>changeStatus(task,e.target.value)} aria-label={`Status for ${task.title}`}>{statuses.map(s=><option key={s}>{s}</option>)}</select><em className={`priority ${task.priority.toLowerCase()}`}>{task.priority}</em></div>{status==="Review"&&<small>Waiting for manager/admin review</small>}</div>})}{!filteredTasks.filter(t=>t.status===status).length&&<div className="empty-state">No tasks here.</div>}</div>)}</div></div><style>{`.task-filters{display:grid;grid-template-columns:repeat(4,minmax(120px,1fr));gap:8px;margin:14px 0}.status-heading{display:flex;align-items:center;gap:7px}.status-heading svg,.task-status-icon{color:#5548c9}.task-status-icon{display:flex}.task-description{font-size:12px!important;color:#777e89!important;line-height:1.4}.task-project{font-weight:700;color:#4f46a5!important}.task-due-line{display:flex!important;align-items:center;gap:4px;font-size:11px!important;color:#656c78!important}.task-card-footer{display:flex;align-items:center;justify-content:space-between;gap:7px;margin-top:4px}.task-status-select{font-size:11px;border:1px solid #dfe3e8;border-radius:7px;padding:5px 7px;background:#fff;color:#343944}.task-status-select:disabled{opacity:.7}.task-card small{color:#8a5b12}.task-board-scroll{width:100%;overflow-x:auto;overflow-y:hidden;padding-bottom:10px;-webkit-overflow-scrolling:touch;scrollbar-width:auto}.task-board-scroll::-webkit-scrollbar{height:9px}.task-columns{display:grid;grid-template-columns:repeat(5,minmax(245px,1fr));gap:12px;min-width:1260px}.task-column{min-width:245px}@media(max-width:600px){.task-filters{grid-template-columns:1fr}.form-row{grid-template-columns:1fr!important}.task-columns{grid-template-columns:repeat(5,minmax(245px,1fr));min-width:1260px}}`}</style></div>}
