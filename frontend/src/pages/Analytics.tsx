import { useEffect, useState } from "react";
import { BarChart3, CheckCircle2, Circle, Clock3, ShieldAlert } from "lucide-react";
import api from "../api";

type Data={stats:{projects:number;tasks:number;completed:number};taskBreakdown:{done:number;inProgress:number;todo:number;review?:number;blocked?:number}};
export default function Analytics(){const[data,setData]=useState<Data|null>(null);useEffect(()=>{api.get("/dashboard/summary").then(({data:result})=>setData(result));},[]);const total=data?.stats.tasks||0;const completion=total?Math.round((data!.stats.completed/total)*100):0;const items=data?[{label:"Todo",value:data.taskBreakdown.todo,icon:Circle},{label:"In progress",value:data.taskBreakdown.inProgress,icon:Clock3},{label:"Review",value:data.taskBreakdown.review||0,icon:BarChart3},{label:"Blocked",value:data.taskBreakdown.blocked||0,icon:ShieldAlert},{label:"Done",value:data.taskBreakdown.done,icon:CheckCircle2}]:[];return <div className="workspace-page"><div className="page-heading"><div><p className="eyebrow">Insights</p><h2>Analytics</h2><p>Understand delivery health from the activity already happening in your workspace.</p></div><span className="role-badge">Live workspace data</span></div><div className="stats-grid"><div className="stat-card"><div className="stat-icon"><BarChart3 size={18}/></div><p>Total tasks</p><h3>{total}</h3></div><div className="stat-card"><div className="stat-icon"><CheckCircle2 size={18}/></div><p>Completion rate</p><h3>{completion}%</h3></div><div className="stat-card"><div className="stat-icon"><BarChart3 size={18}/></div><p>Projects</p><h3>{data?.stats.projects??0}</h3></div><div className="stat-card"><div className="stat-icon"><Clock3 size={18}/></div><p>In progress</p><h3>{data?.taskBreakdown.inProgress??0}</h3></div></div><div className="analytics-grid"><div className="panel"><div className="panel-header"><div><p className="eyebrow">Distribution</p><h3>Tasks by status</h3></div></div><div className="distribution">{items.map(({label,value,icon:Icon})=><div className="distribution-row" key={label}><Icon size={14}/><span>{label}</span><strong>{value}</strong></div>)}{!data&&<div className="empty-state">Loading analytics...</div>}</div></div><div className="panel analytics-panel"><div className="panel-header"><div><p className="eyebrow">Team health</p><h3>Workspace delivery snapshot</h3></div></div>{items.map(({label,value})=><div className="metric-row" key={label}><div className="metric-label"><strong>{label}</strong><span>{value}</span></div><div className="metric-track"><div style={{width:`${total?Math.min(100,value/total*100):0}%`}}/></div></div>)}</div></div></div>}
